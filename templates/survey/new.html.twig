{% extends 'base.html.twig' %}

{% block title %}Create New Survey - AI-Powered Survey Branching{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2 class="mb-0">🆕 Create New Survey</h2>
                </div>
                <div class="card-body">
                    {% for message in app.flashes('error') %}
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}

                    <form method="post">
                        <div class="mb-3">
                            <label for="title" class="form-label">Survey Title <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="title" name="title" required 
                                   placeholder="Enter survey title..." maxlength="255">
                            <div class="form-text">This will be displayed to respondents</div>
                        </div>

                        <div class="mb-4">
                            <label for="description" class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" rows="3" 
                                      placeholder="Optional description of your survey..."></textarea>
                            <div class="form-text">Provide context about the purpose of this survey</div>
                        </div>

                        <div class="alert alert-info" role="alert">
                            <i class="fas fa-info-circle"></i>
                            <strong>What's next?</strong> After creating your survey, you'll be able to:
                            <ul class="mb-0 mt-2">
                                <li>Add questions with various types (text, multiple choice, rating, etc.)</li>
                                <li>Enable AI-powered dynamic branching</li>
                                <li>Set up conditional logic rules</li>
                                <li>Preview and test your survey flow</li>
                            </ul>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-plus"></i> Create Survey
                            </button>
                            <a href="{{ path('app_survey_index') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left"></i> Back to Surveys
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}