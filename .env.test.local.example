# Local test environment template  
# Copy this file to .env.test.local and fill in your test secrets
# .env.test.local is automatically ignored by git for security

# Generate a 64-character secure test secret:
# php -r "echo bin2hex(random_bytes(32)) . PHP_EOL;"
APP_SECRET=your_64_character_test_secret_here_replace_with_actual_value

# Instructions:
# 1. Copy this file: cp .env.test.local.example .env.test.local  
# 2. Generate a secure secret using the command above
# 3. Replace the APP_SECRET value with your generated secret
# 4. Never commit .env.test.local to version control