# Local development environment template
# Copy this file to .env.local and fill in your actual secrets
# .env.local is automatically ignored by git for security

###> symfony/framework-bundle ###
# Generate a 64-character secure secret:
# php -r "echo bin2hex(random_bytes(32)) . PHP_EOL;"
# Or use: php bin/console secrets:generate-keys (if available)
APP_SECRET=your_64_character_secure_secret_here_replace_with_actual_value
###< symfony/framework-bundle ###

# Instructions:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Generate a secure secret using the command above
# 3. Replace the APP_SECRET value with your generated secret
# 4. Never commit .env.local to version control