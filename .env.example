# Environment Configuration Template
# Copy this file to .env.local and configure with your actual values
# DO NOT place production secrets in files that are committed to version control

###> symfony/framework-bundle ###
# Application environment (dev, prod, test)
APP_ENV=prod

# Cryptographically secure secret (minimum 32 characters)
# Generate with: openssl rand -hex 32
# Or use Symfony: php bin/console secrets:generate-keys
APP_SECRET=your_64_character_cryptographically_secure_secret_here

# Database connection string
# Examples:
# PostgreSQL: postgresql://username:password@127.0.0.1:5432/database_name?serverVersion=13&charset=utf8
# MySQL: mysql://username:password@127.0.0.1:3306/database_name?serverVersion=8.0
# SQLite: sqlite:///%kernel.project_dir%/var/data.db
DATABASE_URL="postgresql://username:password@127.0.0.1:5432/survey_prod?serverVersion=13&charset=utf8"

# Optional: CSRF token salt for enhanced security
# Generate with: openssl rand -hex 16
CSRF_TOKEN_SALT=your_32_character_csrf_salt_here
###< symfony/framework-bundle ###

# Add other environment-specific variables below as needed
# Examples:
# MAILER_DSN=smtp://user:pass@smtp.example.com:587
# REDIS_URL=redis://localhost:6379