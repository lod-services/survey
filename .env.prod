# Production Environment Configuration
# 
# IMPORTANT: Generate a secure secret for production using:
# openssl rand -hex 32
#
# DO NOT use this placeholder in production!

###> symfony/framework-bundle ###
APP_ENV=prod
APP_SECRET=YOUR_64_CHAR_SECRET_HERE
###< symfony/framework-bundle ###

# Production Migration Notes:
# 1. Generate secure secret: openssl rand -hex 32
# 2. Replace the APP_SECRET value above with generated secret
# 3. Existing user sessions will be invalidated (users need to re-login)
# 4. Test CSRF protection after deployment
# 5. Monitor application logs for any security warnings