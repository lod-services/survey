name: Claude – Feature Suggestion (Feeling Lucky)

on:
  schedule:
    - cron: '0 1 * * *'  # 03:00 MESZ / 02:00 MEZ
  workflow_dispatch:

concurrency:
  group: claude-feature-suggestion
  cancel-in-progress: false

jobs:
  run-feature-suggestion:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: dpeuscher
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Claude – Feature Suggestion
        env:
          CI_PROVIDER: github
          TICKET_PROVIDER: github
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO_URL: ${{ github.server_url }}/${{ github.repository }}
          MODE: feature_suggestion
          CLAUDE_CREDENTIALS_B64: ${{ secrets.CLAUDE_CREDENTIALS_B64 }}
        run: |
          docker run --rm \
            -e CI_PROVIDER \
            -e TICKET_PROVIDER \
            -e GH_TOKEN \
            -e REPO_URL \
            -e MODE \
            -e CLAUDE_CREDENTIALS_B64 \
            ghcr.io/dpeuscher/claude-code-agent:latest 
