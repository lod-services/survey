# .env.local.template
# Copy this file to .env.local and customize for your local development environment
# .env.local is ignored by git and will override values from .env

###> Local Development Environment ###
# Override APP_ENV if needed (dev/test/prod)
# APP_ENV=dev

# REQUIRED: Generate your own secure secret for local development
# You can generate one with: php -r "echo bin2hex(random_bytes(32)) . PHP_EOL;"
# This must be set in .env.local for the application to work properly
APP_SECRET=0fde2a65a72435513d26af35d2f9e0c427b8cfed9f86a9e520888df26d324cee

# Override APP_DEBUG if needed
# APP_DEBUG=true
###< Local Development Environment ###

###> Local Database Configuration ###
# Configure your local database connection
# Uncomment and modify one of the following:

# For SQLite (simplest for development):
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# For MySQL:
# DATABASE_URL="mysql://username:password@127.0.0.1:3306/database_name?serverVersion=8.0.32&charset=utf8mb4"

# For PostgreSQL (recommended):
DATABASE_URL="postgresql://app:your_secure_password@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# For Docker containers (using compose.yaml), you might use:
# DATABASE_URL="postgresql://app:secure_password_here@database:5432/app?serverVersion=16&charset=utf8"
###< Local Database Configuration ###

###> Docker Compose Configuration ###
# Required for docker-compose database service
POSTGRES_PASSWORD=secure_password_here
POSTGRES_USER=app
POSTGRES_DB=app
###< Docker Compose Configuration ###

###> Additional Local Configuration ###
# Add any other environment variables you need for local development
# Examples:

# MAILER_DSN=smtp://localhost:1025

# REDIS_URL=redis://localhost:6379

# Enable/disable specific features
# FEATURE_FLAG_EXAMPLE=true
###< Additional Local Configuration ###